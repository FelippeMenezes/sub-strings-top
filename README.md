# sub-strings-top
A Ruby method that scans a string and counts how many times words from a given dictionary appear (case-insensitive), returning the results as a hash.  Built to practice strings, arrays, hashes, and basic control flow.
